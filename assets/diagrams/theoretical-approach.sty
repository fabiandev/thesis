%!tikz editor 1.0
\documentclass{article}
\usepackage{tikz}
\usepackage[graphics, active, tightpage]{preview}
\PreviewEnvironment{tikzpicture}

%!tikz preamble begin
\usetikzlibrary{arrows,backgrounds,er,chains}

%!tikz preamble end


\begin{document}
%!tikz source begin
\begin{tikzpicture}[
  ->,
  >=stealth',
  shorten >= 1pt,
  auto,
  node distance=2.3em and 7em,
  font=\footnotesize,
  text centered,
  every entity/.style={
    minimum width=7.2em,
	minimum height=4.3em,
    outer sep=0,
    inner ysep=.5em
  }
  ]
  
  \tikzset{}  
  
  \node [entity] (A) [draw=none, fill=none] {};
  \node at (A.center) [circle] (a) [draw, fill=gray!10, inner sep=0.5em, minimum size=4em] {Start};

  \node [entity] (B) [below = of A] {\begin{tabular}{c}Source Code\\[.2em]\centering{Analyzation}\end{tabular}};
  \node [entity] (C) [below = of B] {Abstraction};
  \node [entity] (D) [below = of C] {Static Checks};
  \node [entity] (E) [right = of D] {AST Scan};
  \node [entity] (F) [above = of E, fill=gray!10] {Transformations};
  \node [entity] (G) [above = of F] {\begin{tabular}{c}Emit Target\\[.2em]\centering{Code}\end{tabular}};
  
  \node [entity] (H) [above = of G, draw=none, fill=none] {};
  \node at (H.center) [circle] (h) [draw, fill=gray!10, inner sep=0.5em, minimum size=4em] {End};

  \path[shorten >=0.3em, shorten <=0.3em,->] (A) edge node {} (B);
  \path[shorten >=0.3em, shorten <=0.3em,->] (B) edge node {} (C);
  \path[shorten >=0.3em, shorten <=0.3em,->] (C) edge node {} (D);
  
  \draw[shorten >=0.3em,shorten <=0.3em,->] (D.south) -- ++(0,-2.0em) -|  (E.south);
  
  \path[shorten >=0.3em, shorten <=0.3em,->] (E) edge node {} (F);
  \path[shorten >=0.3em, shorten <=0.3em,->] (F) edge node {} (G);
  \path[shorten >=0.3em, shorten <=0.3em,->] (G) edge node {} (H);
  
\end{tikzpicture}
%!tikz source end

\end{document}